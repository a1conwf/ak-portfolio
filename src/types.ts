export interface IProjectItem {
  id?: string;
  img: string;
  name: string;
  techList: string[];
  liveLink: string;
  gitRepo: string;
}

export interface ISkillItem {
  id: string;
  title: string;
}
